1.

SELECT SYSDATE "Date"
FROM dual;

2.

SELECT empno, ename, sal, sal+sal*(15/100) "New Salary"
FROM emp;

3.

SELECT empno, ename, sal, sal+sal*(15/100) "New Salary", sal+sal*(15/100) - sal " Increase"
FROM emp;

4.

SELECT ename, hiredate, to_char(next_day(add_months(hiredate, 6) ,'Monday'), 'Day, " the" Ddspth "of" Month, yyyy') "REVIEW"
FROM emp;

5.

SELECT ename, round(months_between(sysdate, hiredate)) "MONTHS_WORKED"
FROM emp
order by months_between(sysdate, hiredate);

6.

SELECT Concat(ename,concat(' earns ', concat(sal ,concat(' monthly but wants ', 3*sal)))) "Dream Salaries"
FROM emp;

7.

SELECT initcap(ename) " Employee name" , length(ename) "Length" 
FROM emp
WHERE ename like 'J%' or ename like 'A%' or ename like 'M%';

8.

SELECT ename, NVL(to_char(comm), 'No Commission') COMM
FROM emp;

9.

SELECT ename,